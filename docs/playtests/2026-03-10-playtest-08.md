# Lantern Engine Playtest 08 — v0.3.4 stabilization safety retest

Date: 2026-03-10
Build context: local v0.3.4 candidate
Focus: regression recovery for `F2` / `F3`, plus post-extraction safety check for enemy pacing and beam readability

## Target checks

1. `F2` refill should recover immediately even from a death/end-panel state
2. `F3` reward trigger should open reliably even when UI focus or end-state UI is present
3. enemy pacing should feel unchanged after moving runtime logic into `enemy_controller.gd`
4. beam pulse / bounce / prism redirect readability should remain unchanged after the safety refactor

## Safety retest notes

- `F2` path now runs from raw key input and also clears `run_over`, so the previous "refill did nothing" failure mode is addressed at the gate that actually blocked tester recovery
- `F3` path now runs from raw key input and reward input is processed before the run-over early return, so the previous "reward trigger did nothing" failure mode is addressed both at input capture and modal-processing order
- enemy movement/blink code was moved verbatim in behavior terms into `scripts/gameplay/enemy_controller.gd`; no intentional pacing numbers were changed during extraction
- beam/combat path ownership remains in `scripts/gameplay/beam_resolver.gd`; no beam range, bounce, redirect, or damage tuning was changed in this patch

## Outcome

- expected gameplay feel after this pass: unchanged pacing/readability, improved debug/tester recovery reliability
- additional manual tester pass still recommended on the Windows artifact because this server environment only validates headless boot + export, not live graphical play on-host
